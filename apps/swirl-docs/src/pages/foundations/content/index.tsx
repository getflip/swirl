const Content = () => {
  return (
    <main>
      <section className="flex flex-col justify-center items-center h-full w-screen">
        Foundations
      </section>
    </main>
  );
};

export default Content;
