import { Controls, Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import * as Stories from "./swirl-table.stories";

<Meta title="Components/SwirlTable" />

# SwirlTable

The SwirlTable component is used to organize and display data sets.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Swirl-Components?node-id=2368%3A11396)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-table)**

## Usage

<Canvas of={Stories.SwirlTable} sourceState="shown"></Canvas>

<Controls of={Stories.SwirlTable} />

## Related components

- **[swirl-table-column](?path=/docs/components-swirltablecolumn--docs)** (child) — Column definitions; use in the `columns` slot of `swirl-table`.
- **[swirl-table-row-group](?path=/docs/components-swirltablerowgroup--docs)** (child) — Optional row grouping; use in the `rows` slot of `swirl-table`.
- **[swirl-table-row](?path=/docs/components-swirltablerow--docs)** (child) — Table rows; use in the `rows` slot of `swirl-table` or inside `swirl-table-row-group`.

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Table Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/table/).

| ARIA                  | Usage                                                           |
| --------------------- | --------------------------------------------------------------- |
| `role="table"`        | Set on the outer table element.                                 |
| `aria-rowcount`       | Set to the overall row count.                                   |
| `aria-label`          | An accessible label is provided via the `aria-label` attribute. |
| `role="rowgroup"`     | Set on the table's header and body elements.                    |
| `role="columnheader"` | Set on the table's column element.                              |
| `aria-sort`           | Set on a sorted table column.                                   |
| `role="row"`          | Set on a the table's row elements.                              |
| `aria-rowindex`       | Set to the overall index of the row element.                    |
| `role="cell"`         | Set on a the table's cell elements.                             |

### Keyboard

No component-specific keyboard behavior; table navigation depends on host or application.
