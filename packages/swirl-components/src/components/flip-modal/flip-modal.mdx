import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

<Meta title="Components/FlipModal" />

# FlipModal

The FlipModal component is used to overlay the user interface with content that
needs the immediate attention from the user.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Swirl-Components)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/flip-modal)**

## Usage

<Canvas withSource="open">
  <Story id="components-flipmodal--flip-modal" />
</Canvas>

<ArgsTable story="." />

## Theming

| Variable                      | Usage                                   |
| ----------------------------- | --------------------------------------- |
| `--s-surface-overlay-default` | Background color of the modal           |
| `--s-border-default`          | Border color of the modal footer/header |

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Dialog Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialogmodal/).

| ARIA            | Usage                                         |
| --------------- | --------------------------------------------- |
| `role="dialog"` |                                               |
| `aria-modal`    | Set to `true` on the modal container element. |
| `aria-label`    | Used to provide the modal's accessible label. |

### Keyboard

| Key            | Action            |
| -------------- | ----------------- |
| <kbd>ESC</kbd> | Closes the modal. |

### Focus handling

- Focus is moved to the first tabbable element when the modal is opened.
- Focus is trapped inside the modal, if it is open.
- After closing, the focus is returned to the last focused element.
