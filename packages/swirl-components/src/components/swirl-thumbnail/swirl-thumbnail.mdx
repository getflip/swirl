import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs/blocks";
import * as Stories from "./swirl-thumbnail.stories";

<Meta title="Components/SwirlThumbnail" />

# SwirlThumbnail

The SwirlThumbnail component is used to visually represent an object via a small
image.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=205%3A4336)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-thumbnail)**

## Usage

<Canvas of={Stories.SwirlThumbnail}></Canvas>

<Controls of={Stories.SwirlThumbnail} />

## Sample usage

<Source
  code={`<swirl-thumbnail alt="Brief description of the image." src="https://picsum.photos/id/433/400/400"></swirl-thumbnail>`}
/>

## Related components

No related components.

## Accessibility

SwirlThumbnail is a presentational image wrapper with no ARIA attributes or
keyboard-specific behavior by default. When `interactive` is enabled, the image
becomes a focusable `<button>`. Action buttons rendered by `show-edit-button`
and `show-remove-button` expose accessible names via the `edit-button-label`
and `remove-button-label` props. On compact sizes, actions are grouped behind a
menu button whose label is set via the `menu-button-label` prop.

### Keyboard

When action buttons or the compact menu button receive keyboard focus, they
become visible. The compact menu button opens a popover that can be navigated
with standard keyboard controls.
