import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs/blocks";
import * as Stories from "./swirl-switch.stories";

<Meta title="Components/SwirlSwitch" />

# SwirlSwitch

The SwirlSwitch component is used as an alternative to the SwirlCheckbox, if an
on/off state is more appropriate than a checked/unchecked state.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=2%3A50)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-switch)**

## Usage

<Canvas of={Stories.SwirlSwitch}></Canvas>

<Source
  code={`<swirl-switch checked input-id="switch" input-name="switch" label="Switch"></swirl-switch>`}
/>

<Controls of={Stories.SwirlSwitch} />

## Related components

No related components.

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Switch Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/switch/).

| ARIA            | Usage                                                                                                                          |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `role="switch"` |                                                                                                                                |
| `aria-label`    | If no accessible label is provided via the switch's label prop, the `aria-label` or `aria-labelledby` attribute has to be set. |
| `aria-checked`  | The switch gets the `aria-checked` attribute set to `true` in "on" state, and to `false` in "off" state.                       |

### Keyboard

| Key              | Action                           |
| ---------------- | -------------------------------- |
| <kbd>SPACE</kbd> | Changes the state of the switch. |
