import { Controls, Canvas, Meta, Story } from "@storybook/addon-docs";
import * as Stories from "./swirl-pdf-reader.stories";

<Meta title="Components/SwirlPDFReader" />

# SwirlPDFReader

The SwirlPDFReader component is used to present a PDF document in a modal
overlay.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Swirl-Components?node-id=495%3A7144)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-pdf-reader)**

## Usage

<Canvas of={Stories.SwirlPdfReader} sourceState="shown"></Canvas>

<Controls of={Stories.SwirlPdfReader} />

## Related components

No related components.

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Dialog Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialogmodal/).

| ARIA                | Usage                                              |
| ------------------- | -------------------------------------------------- |
| `role="dialog"`     | Set on the modal wrapping the lightbox.            |
| `aria-modal="true"` | Set on the modal wrapping the lightbox.            |
| `aria-label`        | Used to provide an accessible label for the modal. |

### Keyboard

| Key                                               | Action                                              |
| ------------------------------------------------- | --------------------------------------------------- |
| <kbd>TAB</kbd>, <kbd>SHIFT</kbd> + <kbd>TAB</kbd> | Move through the interactive elements of the modal. |
| <kbd>ESC</kbd>                                    | Closes the modal.                                   |

### Focus handling

- Focus is moved to the first tabbable element when the modal is opened.
- Focus is trapped inside the modal, if it is open.
- After closing, the focus is returned to the last focused element.
