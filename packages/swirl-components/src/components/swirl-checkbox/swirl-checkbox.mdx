import { Canvas, Controls, Meta, Story, Source } from "@storybook/addon-docs/blocks";
import * as Stories from "./swirl-checkbox.stories";

<Meta title="Components/SwirlCheckbox" />

# SwirlCheckbox

The SwirlCheckbox component is used as a form control to allow a user to select
one or multiple choices from a list of options.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=2%3A18)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-checkbox)**

## Usage

<Canvas of={Stories.SwirlCheckbox}></Canvas>

<Source
  code={`<swirl-checkbox
    checked
    description="Optional description of the item."
    input-id="checkbox"
    input-name="Checkbox"
    label="Checkbox"
></swirl-checkbox>`}
/>

<Controls of={Stories.SwirlCheckbox} />

## Related components

- **[swirl-form-control](?path=/docs/components-swirlformcontrol--docs)**
  (parent) — Wraps the checkbox with label and description; use `swirl-checkbox`
  in the default slot of `swirl-form-control`.

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Checkbox Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/).

| ARIA              | Usage                                                                                                                                                |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `role="checkbox"` |                                                                                                                                                      |
| `aria-label`      | If no accessible label is provided via the checkbox's label prop, the `aria-label` or `aria-labelledby` attribute has to be set.                     |
| `aria-checked`    | The checkbox gets the `aria-checked` attribute set to `true` in checked state, to `false` in unchecked state, and to `mixed` in indeterminate state. |

### Keyboard

| Key              | Action                             |
| ---------------- | ---------------------------------- |
| <kbd>SPACE</kbd> | Changes the state of the checkbox. |
