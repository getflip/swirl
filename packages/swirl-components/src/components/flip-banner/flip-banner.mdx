import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

<Meta title="Components/FlipBanner" />

# FlipBanner

The FlipBanner component is used to display a prominent message at the top of
the screen.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=2%3A14)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/flip-banner)**

## Usage

<Canvas withSource="open">
  <Story id="components-flipbanner--flip-banner" />
</Canvas>

<ArgsTable story="." />

## Theming

| Variable                       | Usage                                                          |
| ------------------------------ | -------------------------------------------------------------- |
| `--s-background-default`       | Background color for intent "default"                          |
| `--s-surface-neutral-default`  | Background color for intent "neutral"                          |
| `--s-surface-critical-default` | Background color for intent "critical"                         |
| `--s-surface-success-default`  | Background color for intent "success"                          |
| `--s-surface-warning-default`  | Background color for intent "warning"                          |
| `--s-surface-info-default`     | Background color for intent "info"                             |
| `--s-border-default`           | Border color for intent "default"                              |
| `--s-text-subdued`             | Text color for intent "default"                                |
| `--s-text-default`             | Text color for intent "warning"                                |
| `--s-text-on-status`           | Text color for intent "neutral", "critical", "success", "info" |
| `--s-text-highlight`           | Text color action button on intent "default"                   |

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Alert Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/alert/).

| ARIA               | Usage                                                                                         |
| ------------------ | --------------------------------------------------------------------------------------------- |
| `role="alert"`     | The banner gets the `alert` role if it contains critical info or warnings.                    |
| `role="status"`    | The banner gets the `status` role for non-critical info.                                      |
| `aria-describedby` | The `aria-describedby` property is automatically set to reference the banner's title or body. |
