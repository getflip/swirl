import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { extractArgTypes } from "@pxtrn/storybook-addon-docs-stencil";

<Meta title="Components/FlipButton" />

# FlipButton

The FlipButton component is used to trigger actions or events. It is **not**
used if a link is more appropriate.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=2%3A15)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/flip-button)**

## Usage

<Canvas withSource="open">
  <Story id="components-flipbutton--flip-button" />
</Canvas>

<ArgsTable story="." />

## Theming

### Ghost variant

| Variable                 | Usage                                            |
| ------------------------ | ------------------------------------------------ |
| `--s-background-default` | Background color of the button                   |
| `--s-background-hovered` | Background color of the hovered button           |
| `--s-background-pressed` | Background color of the active button            |
| `--s-text-subdued`       | Text color of the button                         |
| `--s-icon-default`       | Icon color of the button                         |
| `--s-background-default` | Background color of the disabled button          |
| `--s-text-disabled`      | Text color of the disabled button                |
| `--s-icon-disabled`      | Icon color of the disabled button                |
| `--s-text-critical`      | Text color of the button with intent "critical"  |
| `--s-icon-critical`      | Icon color of the button with intent "critical"  |
| `--s-text-highlight`     | Text color of the button with intent "highlight" |
| `--s-icon-highlight`     | Icon color of the button with intent "highlight" |

## Accessibility

### ARIA

The component follows the
[WAI-ARIA Button Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/button/).

| ARIA            | Usage                                                                                                             |
| --------------- | ----------------------------------------------------------------------------------------------------------------- |
| `role="button"` | Implicitly through the use of <code>&lt;button /></code>.                                                         |
| `aria-label`    | If no accessible label is provided via the button's text, the `aria-label` or `aria-labelledby` attribute is set. |
| `aria-disabled` | If the button is disabled, the `aria-disabled` attribute is set to `true`.                                        |

### Keyboard

| Key              | Action                |
| ---------------- | --------------------- |
| <kbd>ENTER</kbd> | Activates the button. |
| <kbd>SPACE</kbd> | Activates the button. |
