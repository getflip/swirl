import { Controls, Canvas, Meta, Story } from "@storybook/addon-docs";
import * as Stories from "./swirl-pagination.stories";

<Meta title="Components/SwirlPagination" />

# SwirlPagination

The SwirlPagination component is used to to split up content into pages and
navigate between them.

- **[Figma Design Specs](https://www.figma.com/file/iRHiAFsHxnzux8gQ9EjlZe/Flip-Components?node-id=2%3A38)**
- **[Source Code](https://github.com/flip-corp/swirl/tree/main/packages/swirl-components/src/components/swirl-pagination)**

## Usage

<Canvas of={Stories.SwirlPagination} sourceState="shown"></Canvas>

<Controls of={Stories.SwirlPagination} />

## Accessibility

### ARIA

The component uses a native HTML `nav` element. A label has to be provided to
make the pagination distinguishable from other navigation blocks and the app's
main navigation.

| ARIA                  | Usage                                                  |
| --------------------- | ------------------------------------------------------ |
| `role="navigation"`   | Implicit through the use of the `nav` element.         |
| `aria-current="page"` | Set on the current page label.                         |
| `aria-label`          | Set to provide an accessible label for the pagination. |

### Keyboard

No component-specific keyboard behavior; page links and buttons use standard link/button semantics.
