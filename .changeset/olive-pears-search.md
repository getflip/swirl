---
"@getflip/swirl-components": patch
"@getflip/swirl-components-angular": patch
"@getflip/swirl-components-react": patch
---

`swirl-modal` no longer moves focus back to `input[autofocus]` if focus already moved elsewhere inside the modal
